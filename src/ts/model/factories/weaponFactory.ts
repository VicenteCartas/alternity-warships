import { WeaponComponent } from "../baseComponents/weaponComponent";

export class WeaponFactory {
    public static getDefaults(): WeaponComponent[] {
        const weapons: WeaponComponent[] = [];

        return weapons;
    }
}
