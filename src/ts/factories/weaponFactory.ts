import { WeaponComponent } from "../components/weaponComponent";

export class WeaponFactory {
    public static getDefaults(): WeaponComponent[] {
        let weapons: WeaponComponent[] = [];

        return weapons;
    }
}